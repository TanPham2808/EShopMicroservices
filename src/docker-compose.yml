version: '3.4'

services:
  catalog.api:
    image: ${DOCKER_REGISTRY-}catalogapi
    build:
      context: .
      dockerfile: Services/Catalog/Catalog.API/Dockerfile

  catalogdb:
    image: postgres   # Lấy image Postgres từ Docker Hub xuống

volumes:
  postgres_catalog:
